{
  "name": "@three-soft/app-http",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "lint": "eslint .  --no-fix --max-warnings 0",
    "test": "NODE_ENV=test jest --runInBand --detectOpenHandles --verbose",
    "test:coverage": "NODE_ENV=test jest --runInBand --detectOpenHandles --coverage",
    "test:integration": "NODE_ENV=test jest --testPathPattern=tests/integration --runInBand --detectOpenHandles --verbose",
    "test:unit": "NODE_ENV=test jest --testPathPattern=tests/unit --runInBand --detectOpenHandles --verbose"
  },
  "dependencies": {
    "@fastify/static": "6.5.0",
    "@nestjs/common": "9.0.8",
    "@nestjs/core": "9.0.8",
    "@nestjs/platform-fastify": "9.0.8",
    "reflect-metadata": "0.1.13",
    "rxjs": "7.5.6"
  },
  "devDependencies": {
    "@fastify/swagger": "7.4.1",
    "@nestjs/cli": "9.0.0",
    "@nestjs/testing": "9.0.8",
    "@types/express": "4.17.13"
  }
}
